/* eslint-disable @typescript-eslint/no-unused-vars */
import * as entities from "entities";

import * as ui from "shared/ui";

import "./app/app";

// NOTE: просто используются все файлы компонентов, чтобы в них отработал registerComponent(Component)
const registerComponents_ui = ui;
const registerComponents_entities = entities;
