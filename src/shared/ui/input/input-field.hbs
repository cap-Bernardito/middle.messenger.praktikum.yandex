<input
  class="form-control__input {{classNameInput}}"
  type="{{type}}"
  placeholder="{{placeholder}}"
  name="{{name}}"
  value="{{value}}"
  {{#if required}} required {{/if}}
  {{#if disabled}} disabled {{/if}}
>
